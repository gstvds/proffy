import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
  },

  teacherList: {
    marginTop: -60,
  }
});

export default styles;